# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Secrets — never commit API keys
.env
*.key

# Local database (pre-PostgreSQL leftovers)
dating.db

# Local dev state — Postgres lives in a Docker volume, not the working tree
.requirements-stamp
